const login = require('./src/api/login');
const weather = require('./src/api/weather');
const finance = require('./src/api/finance');
const bill = require('./src/api/bill');

module.exports = {
    login,
    weather,
    finance,
    bill
}